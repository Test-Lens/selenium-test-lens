# Integrations

Lens is runner-agnostic and attaches to an already-created driver. See [existing WebDriver integration](../framework-integration.md) for JUnit, TestNG, Page Objects, Allure coexistence, and parallel-isolation patterns.

The [Optional React API](react.md) is the only framework-specific published module.
