/**
 * Provides explicit, synchronous transport of completed Test Lens report ZIPs.
 * Upload is separate from session finalization and never owns a WebDriver.
 */
package io.github.testlens.selenium.reporting;
